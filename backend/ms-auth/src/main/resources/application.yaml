spring:
  application:
    name: ms-auth

  datasource:
    url: jdbc:mysql://localhost:3306/db_elas_tech
    username: root
    password: aluno1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  flyway:
    enabled: true
    locations: classpath:db/migration
server:
  port: 8081

app:
  auth:
    issuer: http://localhost:8081
    access-token-expiration-minutes: 30
    usuarios-base-url: http://localhost:8082